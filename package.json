{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "preinstall": "npm run clean",
    "install": "lerna bootstrap --npmClient=yarn",
    "start": "lerna run --stream start"
  },
  "devDependencies": {
    "lerna": "^3.0.0-rc.0",
    "prettier": "^1.14.0"
  }
}
